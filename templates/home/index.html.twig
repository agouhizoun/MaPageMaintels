{% extends 'layout.html.twig' %}

{% block title %}{{ parent() }} Hello Ma Page{% endblock %}


{% block content %}

<div class="text-center mb-5">
    <h2>Bienvenue à la page des prestations de Services Informatique et Télécom</h2>
    <h4>Voici un bref aperçu de nos prestations. Aller dans l'onglet Panier pour plus de choix et continuer votre shopping :</h4>
</div>

{# Chaque prestation à sa card et pour la taille des images, il faut diminuer style="max-width: 600px;"  #}
<div class="justify-content-center">
    {% for prestation in prestations %}
    <div class="row justify-content-center">
        <div class="col card mb-3 " style="max-width: 600px;">
            <div class="row g-0">
                <div class="col-md-6 justify-content-center">
                    <img src="{{ asset('photosProduits/' ~ prestation.photo) }}" class="img-fluid rounded-start"
                        alt="{{ prestation.nom }}">
                </div>
                <div class="col-md-6">
                    <div class="card-body text-center">
                        <h4 class="card-title"><a class="text-decoration-none text-success" href="{{ path('prestation_show', {id: prestation.id}) }}">{{prestation.nom}}</a></h4>
                        <h6 class="text-muted">ajouté le : {{ prestation.dateEnregistrement | date('d/m/Y') }}</h6>
                        <h5>prix : <strong>{{ prestation.prixPrestation }}€</strong></h5>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {% endfor %}

</div>

{% endblock content %}
